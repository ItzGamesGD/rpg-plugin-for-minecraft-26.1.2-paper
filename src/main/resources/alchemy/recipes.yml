# Each entry is one vanilla-style Brewing Stand transition:
# base-input-id in a bottle slot + ingredient-id in the ingredient slot -> result-potion-id.
# Catalysts use the same staged transition model and never require hidden slots.
data_version: 2
recipes: {}
