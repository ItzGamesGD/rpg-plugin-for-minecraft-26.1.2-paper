schema-version: 1
enabled: false

# Effects and limits are configurable placeholders until the balance stage.
tokens:
  vitality:
    item-id: vitality_stat_token
    maximum-uses: 1
    effect: MAX_HEALTH
    effect-amount: 0.0
    account-bound: true
  satiety:
    item-id: satiety_stat_token
    maximum-uses: 1
    effect: SATIETY_DECAY
    effect-amount: 0.0
    account-bound: true
  abundance:
    item-id: abundance_stat_token
    maximum-uses: 1
    effect: SEED_CHANCE
    effect-amount: 0.0
    account-bound: true
