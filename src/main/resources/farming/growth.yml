schema-version: 1
enabled: true

scheduler:
  tick-seconds: 1
  save-seconds: 15

debug:
  # Temporary event diagnostics for live farming verification.
  events: false

# Temporary Stage 1 values. Balance and final growth timing are later work.
crops:
  corn:
    seconds-per-stage: 60
  onion:
    seconds-per-stage: 60
  chili:
    seconds-per-stage: 60
  garlic:
    seconds-per-stage: 60
