name: Build and Pre-release HyunseoRPG Plugin

on:
  push:
    branches:
      - ci/build-plugin-artifact
  workflow_dispatch:

permissions:
  contents: write

env:
  RELEASE_TAG: v0.1.0-dev.1
  TEST_GUIDE: RELEASE_TEST_GUIDE-v0.1.0-dev.1.md

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout main source
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build and verify plugin
        run: ./gradlew clean build --no-daemon --stacktrace

      - name: List produced JARs
        run: ls -lah build/libs

      - name: Upload plugin JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: HyunseoRPG-plugin
          path: build/libs/HyunseoRPG-*.jar
          if-no-files-found: error
          retention-days: 7

      - name: Fetch release test guide from CI branch
        run: |
          git fetch origin ci/build-plugin-artifact
          git show origin/ci/build-plugin-artifact:"$TEST_GUIDE" > "$TEST_GUIDE"

      - name: Create or update GitHub pre-release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          MAIN_SHA="$(git rev-parse HEAD)"
          JAR_PATH="$(find build/libs -maxdepth 1 -type f -name 'HyunseoRPG-*.jar' | head -n 1)"
          test -n "$JAR_PATH"
          test -s "$TEST_GUIDE"
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" "$JAR_PATH" "$TEST_GUIDE" --clobber
          else
            gh release create "$RELEASE_TAG" "$JAR_PATH" "$TEST_GUIDE" \
              --target "$MAIN_SHA" \
              --title "HyunseoRPG $RELEASE_TAG" \
              --notes "Development pre-release built and verified automatically from main. Includes a live-test command guide." \
              --prerelease
          fi
